# =============================================================================
# Usual Store Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# This project supports both IPv4 and IPv6 networking

# -----------------------------------------------------------------------------
# Application Ports
# -----------------------------------------------------------------------------
USUAL_STORE_PORT=4000
API_PORT=4001
INVOICE_PORT=5000

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# For Docker environment (container-to-container):
# Use the service name 'database' - Docker DNS handles both IPv4/IPv6
DATABASE_DSN=postgres://postgres:password@database:5432/usualstore?sslmode=disable

# For local development (connecting from host to Docker):
# IPv6 format:
# DATABASE_DSN=postgres://postgres:password@[::1]:5432/usualstore?sslmode=disable
# 
# IPv4 format (backward compatible):
# DATABASE_DSN=postgres://postgres:password@127.0.0.1:5432/usualstore?sslmode=disable

# -----------------------------------------------------------------------------
# Stripe Payment Configuration
# -----------------------------------------------------------------------------
STRIPE_KEY=your_stripe_publishable_key_here
STRIPE_SECRET=your_stripe_secret_key_here

# -----------------------------------------------------------------------------
# Application URLs
# -----------------------------------------------------------------------------
# For IPv6 localhost:
# FRONT_URL=http://[::1]:4000
# API_URL=http://[::1]:4001
# 
# For IPv4 localhost:
FRONT_URL=http://localhost:4000
API_URL=http://localhost:4001

# -----------------------------------------------------------------------------
# Security
# -----------------------------------------------------------------------------
SECRET_FOR_FRONT=your_secret_key_at_least_32_chars_long_here

# -----------------------------------------------------------------------------
# Email Configuration (SMTP)
# -----------------------------------------------------------------------------
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=your_mailtrap_username
SMTP_PASSWORD=your_mailtrap_password

# -----------------------------------------------------------------------------
# IPv6 Network Configuration Notes
# -----------------------------------------------------------------------------
# Docker IPv6 Subnet: fd00:dead:beef::/48
# Docker IPv4 Subnet: 172.20.0.0/16
# 
# Services will be accessible via:
# - IPv6: [fd00:dead:beef::x]:port
# - IPv4: 172.20.0.x:port
# - Container name: service-name:port (recommended)
# 
# Host machine access:
# - IPv6 localhost: [::1]:port
# - IPv4 localhost: 127.0.0.1:port or localhost:port
